# Single source of truth for versions is pyproject.toml. This installs the
# headless HPC/GPU stack (torch + h5py, no Qt UI) — equivalent to
# `pip install .[hpc]`. For CUDA wheels, install torch from the appropriate
# PyTorch index first, then `pip install -r requirements_hpc.txt`.
.[hpc]
