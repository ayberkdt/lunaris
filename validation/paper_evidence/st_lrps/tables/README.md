# tables/

Paper tables and the multi-seed summary, regenerated from CSV outputs by
`--stage tables` and `--stage multi-seed`: `table_*.md` (dataset/split, field
validation, orbit benchmark, runtime/speedup, worst-case, multi-seed, ablation)
and `multi_seed_summary.csv` / `.md`. Numbers come from the CSVs, not hardcoded.
